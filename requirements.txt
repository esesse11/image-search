# Image Search System - Dependencies
# Install: pip install -r requirements.txt

# Web Framework
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
streamlit>=1.28.0
python-multipart>=0.0.5

# Data & DB
sqlalchemy>=2.0.0
# sqlite3: Built-in Python module, no installation needed
pydantic>=2.5.0
pydantic-settings>=2.0.0

# Vector Search
faiss-cpu>=1.8.0  # For CPU; use faiss-gpu if CUDA available
qdrant-client>=1.10.0  # Optional: Qdrant vector DB

# ML & Vision
openai>=1.3.0  # OpenAI API (embeddings, vision, fine-tuning)
pillow>=10.0.0  # Image processing
numpy>=1.26.0  # Compatible with Python 3.12
scikit-learn>=1.3.0

# Image Processing & Color Extraction
opencv-python==4.8.1.78
# colorsys: Built-in Python module, no installation needed
scipy==1.11.4  # For palette analysis

# Data & Config
pyyaml==6.0.1
python-dotenv==1.0.0
pandas==2.1.3

# Utilities
requests==2.31.0
tqdm==4.66.1
loguru==0.7.2

# Development (Optional)
black==23.12.0
flake8==6.1.0
pytest==7.4.3
pytest-cov==4.1.0

# Notes:
# - faiss-cpu is for CPU-only machines
# - If CUDA is available, replace with: faiss-gpu
# - Pillow requires system libs: apt-get install libjpeg-dev zlib1g-dev (Linux)
# - All packages are pip-installable; no system Docker needed
