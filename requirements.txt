# Image Search System - Dependencies
# Install: pip install -r requirements.txt

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
streamlit==1.28.1
python-multipart==0.0.6

# Data & DB
sqlalchemy==2.0.23
sqlite3-python==1.0.0  # SQLite support (fallback, bundled in Python)
pydantic==2.5.0
pydantic-settings==2.1.0

# Vector Search
faiss-cpu==1.7.4  # For CPU; use faiss-gpu if CUDA available
qdrant-client==2.7.0  # Optional: Qdrant vector DB

# ML & Vision
openai==1.3.9  # OpenAI API (embeddings, vision, fine-tuning)
pillow==10.1.0  # Image processing
numpy==1.24.3
scikit-learn==1.3.2

# Image Processing & Color Extraction
opencv-python==4.8.1.78
colorsys==  # Built-in, no install needed
scipy==1.11.4  # For palette analysis

# Data & Config
pyyaml==6.0.1
python-dotenv==1.0.0
pandas==2.1.3

# Utilities
requests==2.31.0
tqdm==4.66.1
loguru==0.7.2

# Development (Optional)
black==23.12.0
flake8==6.1.0
pytest==7.4.3
pytest-cov==4.1.0

# Notes:
# - faiss-cpu is for CPU-only machines
# - If CUDA is available, replace with: faiss-gpu
# - Pillow requires system libs: apt-get install libjpeg-dev zlib1g-dev (Linux)
# - All packages are pip-installable; no system Docker needed
